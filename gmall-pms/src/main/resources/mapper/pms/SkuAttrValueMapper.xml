<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.gmall.pms.mapper.SkuAttrValueMapper">

    <select id="querySkuAttrValuesBySpuId" resultType="com.atguigu.gmall.sms.vo.AttrValueVo">
        SELECT
            a.attr_id,a.attr_name,a.attr_value
        FROM
            pms_sku_attr_value a
        INNER JOIN pms_sku b ON a.sku_id = b.id
        WHERE
            b.spu_id = #{spuId}
    </select>

    <select id="querySkuJsonsBySpuId" resultType="java.util.Map">
        select sku_id, GROUP_CONCAT(attr_value ORDER BY attr_id asc ) as attrValues
        from pms_sku_attr_value
        <trim prefix="where" prefixOverrides="and|or" suffix="GROUP BY sku_id" suffixOverrides=",">
            and sku_id in
            <foreach collection="skuIds" item="skuId" separator="," open="(" close=")">
                #{skuId}
            </foreach>
             ,
        </trim>
    </select>
</mapper>
